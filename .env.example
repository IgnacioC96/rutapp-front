# URL base de la API. En dev podés dejar /api/v1 (usa el proxy de Vite).
# En producción, la URL completa del backend, ej: https://tu-api.com/api/v1
VITE_API_URL=/api/v1

# "true" para usar el mock en memoria (sin backend). "false" para API real.
VITE_USE_MOCK=false